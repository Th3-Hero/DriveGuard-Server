<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        style="@style/primary_button"
        android:id="@+id/previousTrips"
        android:contentDescription="@string/description_trip_history_button"
        android:src="@drawable/icon_trip_history"
        android:layout_width="82dp"
        android:layout_height="48dp"
        android:layout_marginEnd="118dp"
        android:layout_marginBottom="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageButton
        android:id="@+id/homeButton"
        style="@style/primary_button"
        android:contentDescription="@string/description_dashboard"
        android:src="@drawable/icon_dashboard"
        android:layout_width="82dp"
        android:layout_height="48dp"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="28dp"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/help"
        style="@style/primary_button"
        android:contentDescription="@string/description_help_button"
        android:src="@drawable/icon_help"
        android:layout_width="82dp"
        android:layout_height="48dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageButton
        android:id="@+id/trip"
        style="@style/primary_button"
        android:contentDescription="@string/description_trip_button"
        android:src="@drawable/icon_car"
        android:layout_width="82dp"
        android:layout_height="48dp"
        android:layout_marginStart="118dp"
        android:layout_marginBottom="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>